server:
  port: 9000
spring:
  config:
    import: "optional:configserver:http://localhost:8071/"
  application:
    name: "cards"
  profiles:
    active: "prod"
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/cardsdb
    username: root
    password: root1234
  jpa:
    show-sql: true
  sql:
    init:
      mode: always

management:
  endpoints:
    web:
      exposure:
        include: "*"